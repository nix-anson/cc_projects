# Scrapy project configuration
# Required for `scrapy list`, `scrapy shell`, and other CLI commands.
# Primary workflow: use `manage.py run_spider <name>` instead of `scrapy crawl`.

[settings]
default = scrapy_spiders.settings

[deploy]
project = tracker_spiders
